[pytest]
pythonpath = .
testpaths = tests
markers =
    needs_server: requires a built valkey-server binary (set VALKEY_SERVER or put on PATH)
    needs_benchmark: requires the extended valkey-benchmark binary (set VALKEY_BENCHMARK or put on PATH)
